import { Link, useNavigate } from "react-router-dom";
import { AnimatePresence, motion as Motion } from "framer-motion";
import { useCart } from "../context/CartContext";
import { useTheme } from "../context/ThemeContext";
import Logo from "../components/Logo";
import { HiOutlineArrowLeft, HiOutlineCheckCircle, HiOutlineMinus, HiOutlineMoon, HiOutlinePlus, HiOutlineShoppingBag, HiOutlineSun, HiOutlineTrash } from "react-icons/hi2";

const themes = {
  dark: { bg: "#080e0a", panel: "#111c14", alt: "#0d1710", text: "#ecfdf5", muted: "#8ab798", faint: "rgba(138,183,152,.55)", line: "rgba(34,197,94,.11)", lineMed: "rgba(34,197,94,.23)", accent: "#32ad63", soft: "rgba(50,173,99,.11)", danger: "#ff6b6b", dangerSoft: "rgba(255,107,107,.1)", glass: "rgba(8,14,10,.9)", shadow: "0 24px 65px rgba(0,0,0,.35)" },
  light: { bg: "#f3f7f4", panel: "#fff", alt: "#f7faf8", text: "#132219", muted: "#587061", faint: "rgba(88,112,97,.62)", line: "rgba(25,76,42,.1)", lineMed: "rgba(25,76,42,.2)", accent: "#278a50", soft: "rgba(39,138,80,.09)", danger: "#dc4c4c", dangerSoft: "rgba(220,76,76,.08)", glass: "rgba(255,255,255,.9)", shadow: "0 24px 65px rgba(19,54,30,.1)" },
};

export default function CartPage() {
  const { dark, toggleTheme } = useTheme();
  const T = dark ? themes.dark : themes.light;
  const { items, removeItem, updateQuantity, clearCart } = useCart();
  const navigate = useNavigate();
  const totalItems = items.reduce((sum, item) => sum + item.quantity, 0);
  const vars = { "--c-bg": T.bg, "--c-panel": T.panel, "--c-alt": T.alt, "--c-text": T.text, "--c-muted": T.muted, "--c-faint": T.faint, "--c-line": T.line, "--c-line-med": T.lineMed, "--c-accent": T.accent, "--c-soft": T.soft, "--c-danger": T.danger, "--c-danger-soft": T.dangerSoft, "--c-glass": T.glass, "--c-shadow": T.shadow };

  return <div className="cart-page" style={vars}>
    <style>{`
      .cart-page{min-height:100dvh;background:var(--c-bg);color:var(--c-text);font-family:'Manrope',system-ui,sans-serif;transition:.2s}.cart-topbar{position:sticky;top:0;z-index:50;height:72px;border-bottom:1px solid var(--c-line);background:var(--c-glass);backdrop-filter:blur(18px)}.cart-topbar-inner{width:min(1180px,100%);height:100%;margin:auto;padding:0 24px;display:flex;align-items:center;justify-content:space-between}.cart-brand,.cart-actions{display:flex;align-items:center}.cart-brand{gap:14px}.cart-brand-copy{padding-left:14px;border-left:1px solid var(--c-line)}.cart-brand-copy strong,.cart-brand-copy span{display:block}.cart-brand-copy strong{font-size:14px}.cart-brand-copy span{margin-top:2px;color:var(--c-muted);font-size:10px}.cart-actions{gap:9px}.cart-icon{width:40px;height:40px;display:grid;place-items:center;border:1px solid var(--c-line);border-radius:11px;background:var(--c-panel);color:var(--c-muted);cursor:pointer}.cart-icon:hover{border-color:var(--c-line-med);color:var(--c-accent)}.cart-icon svg{width:19px}.cart-content{width:min(1100px,100%);margin:auto;padding:48px 24px 90px}.cart-heading{display:flex;align-items:end;justify-content:space-between;gap:20px;margin-bottom:26px}.cart-eyebrow{margin:0 0 7px;color:var(--c-accent);font-size:10px;font-weight:800;letter-spacing:.12em;text-transform:uppercase}.cart-heading h1{margin:0;font-size:clamp(28px,4vw,42px);letter-spacing:-.04em}.cart-heading p{margin:8px 0 0;color:var(--c-muted);font-size:13px}.cart-clear{padding:9px 12px;border:1px solid var(--c-line);border-radius:10px;background:transparent;color:var(--c-muted);font:inherit;font-size:11px;font-weight:700;cursor:pointer}.cart-clear:hover{border-color:var(--c-danger);color:var(--c-danger);background:var(--c-danger-soft)}.cart-layout{display:grid;grid-template-columns:minmax(0,1.55fr) minmax(300px,.75fr);gap:24px;align-items:start}.cart-list{display:grid;gap:12px}.cart-item{display:grid;grid-template-columns:108px minmax(0,1fr) auto;gap:18px;align-items:center;padding:16px;border:1px solid var(--c-line);border-radius:20px;background:var(--c-panel);box-shadow:0 4px 18px rgba(0,0,0,.035)}.cart-image{width:108px;height:96px;object-fit:cover;border-radius:15px;background:var(--c-alt)}.cart-item h2{margin:0;font-size:15px}.cart-source{margin:5px 0 13px;color:var(--c-muted);font-size:11px}.cart-meta{display:flex;align-items:center;gap:10px;flex-wrap:wrap}.cart-free{padding:4px 8px;border-radius:8px;background:var(--c-soft);color:var(--c-accent);font-size:9px;font-weight:800;letter-spacing:.08em}.quantity{display:flex;align-items:center;padding:3px;border:1px solid var(--c-line);border-radius:11px;background:var(--c-alt)}.quantity button{width:29px;height:29px;display:grid;place-items:center;border:0;border-radius:8px;background:transparent;color:var(--c-muted);cursor:pointer}.quantity button:hover{background:var(--c-soft);color:var(--c-accent)}.quantity svg{width:14px}.quantity span{min-width:30px;text-align:center;font-size:12px;font-weight:800}.cart-remove{align-self:start;width:34px;height:34px;display:grid;place-items:center;border:0;border-radius:10px;background:transparent;color:var(--c-faint);cursor:pointer}.cart-remove:hover{background:var(--c-danger-soft);color:var(--c-danger)}.cart-remove svg{width:17px}.cart-summary{position:sticky;top:96px;padding:22px;border:1px solid var(--c-line);border-radius:22px;background:var(--c-panel);box-shadow:var(--c-shadow)}.summary-icon{width:44px;height:44px;display:grid;place-items:center;border-radius:14px;background:var(--c-soft);color:var(--c-accent)}.summary-icon svg{width:22px}.cart-summary h2{margin:16px 0 5px;font-size:18px}.summary-intro{margin:0 0 20px;color:var(--c-muted);font-size:11px;line-height:1.55}.summary-lines{display:grid;gap:12px;padding:16px 0;border-block:1px solid var(--c-line)}.summary-line{display:flex;justify-content:space-between;color:var(--c-muted);font-size:12px}.summary-line strong{color:var(--c-text)}.summary-total{display:flex;justify-content:space-between;align-items:end;padding:18px 0}.summary-total span{font-size:12px;font-weight:700}.summary-total strong{color:var(--c-accent);font-size:22px}.checkout,.browse{width:100%;box-sizing:border-box;display:flex;align-items:center;justify-content:center;gap:9px;padding:14px 18px;border:0;border-radius:13px;background:var(--c-accent);color:#fff;text-decoration:none;font:inherit;font-size:13px;font-weight:800;cursor:pointer;box-shadow:0 10px 25px color-mix(in srgb,var(--c-accent) 27%,transparent)}.checkout:hover{filter:brightness(.94);transform:translateY(-1px)}.checkout-note{display:flex;justify-content:center;gap:6px;margin:13px 0 0;color:var(--c-faint);font-size:9.5px}.checkout-note svg{width:14px;color:var(--c-accent)}.cart-empty{min-height:calc(100dvh - 72px);display:grid;place-items:center;padding:30px}.empty-card{width:min(440px,100%);box-sizing:border-box;padding:40px;text-align:center;border:1px solid var(--c-line);border-radius:26px;background:var(--c-panel);box-shadow:var(--c-shadow)}.empty-icon{width:76px;height:76px;display:grid;place-items:center;margin:0 auto 22px;border-radius:23px;background:var(--c-soft);color:var(--c-accent)}.empty-icon svg{width:34px}.empty-card h1{margin:0;font-size:25px}.empty-card p{margin:10px auto 24px;max-width:290px;color:var(--c-muted);font-size:13px;line-height:1.6}.mobile-checkout{display:none}.cart-icon:focus-visible,.cart-clear:focus-visible,.quantity button:focus-visible,.cart-remove:focus-visible,.checkout:focus-visible,.browse:focus-visible{outline:3px solid var(--c-soft);outline-offset:2px}
      @media(max-width:780px){.cart-topbar{height:64px}.cart-topbar-inner{padding:0 14px}.cart-logo{display:none}.cart-brand-copy{padding:0;border:0}.cart-content{padding:30px 14px 110px}.cart-heading{align-items:start}.cart-heading h1{font-size:28px}.cart-layout{display:block}.cart-summary{position:static;margin-top:18px}.cart-item{grid-template-columns:82px minmax(0,1fr) auto;gap:12px;padding:12px}.cart-image{width:82px;height:82px}.cart-summary .checkout{display:none}.mobile-checkout{display:block;position:fixed;z-index:45;inset:auto 0 0;padding:10px 14px max(10px,env(safe-area-inset-bottom));border-top:1px solid var(--c-line);background:var(--c-glass);backdrop-filter:blur(18px)}}
      @media(max-width:430px){.cart-item{grid-template-columns:68px minmax(0,1fr) 30px}.cart-image{width:68px;height:76px}.cart-free{display:none}.empty-card{padding:30px 22px}}
    `}</style>

    <header className="cart-topbar"><div className="cart-topbar-inner"><div className="cart-brand"><div className="cart-logo"><Logo size={36} /></div><div className="cart-brand-copy"><strong>Your rescue cart</strong><span>{totalItems ? `${totalItems} ${totalItems === 1 ? "item" : "items"} selected` : "Ready when you are"}</span></div></div><div className="cart-actions"><button className="cart-icon" onClick={() => navigate(-1)} aria-label="Go back"><HiOutlineArrowLeft /></button><button className="cart-icon" onClick={toggleTheme} aria-label={dark ? "Use light mode" : "Use dark mode"} title={dark ? "Light mode" : "Dark mode"}>{dark ? <HiOutlineSun /> : <HiOutlineMoon />}</button></div></div></header>

    {!items.length ? <main className="cart-empty"><Motion.div className="empty-card" initial={{ opacity: 0, y: 18 }} animate={{ opacity: 1, y: 0 }}><div className="empty-icon"><HiOutlineShoppingBag /></div><h1>Your cart is waiting</h1><p>Browse available food donations and add what your community needs.</p><Link className="browse" to="/dashboard/search">Browse available food</Link></Motion.div></main> : <main className="cart-content">
      <div className="cart-heading"><div><p className="cart-eyebrow">Review your request</p><h1>Rescue cart</h1><p>Confirm quantities before arranging collection.</p></div><button className="cart-clear" onClick={clearCart}>Clear cart</button></div>
      <div className="cart-layout"><section className="cart-list" aria-label="Cart items"><AnimatePresence initial={false}>{items.map(item => <Motion.article className="cart-item" key={item.id} layout initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, x: -30 }}><img className="cart-image" src={item.image} alt="" /><div><h2>{item.name}</h2><p className="cart-source">From {item.restaurant || "a verified donor"}</p><div className="cart-meta"><div className="quantity" aria-label={`Quantity for ${item.name}`}><button onClick={() => item.quantity > 1 ? updateQuantity(item.id, item.quantity - 1) : removeItem(item.id)} aria-label="Decrease quantity"><HiOutlineMinus /></button><span aria-live="polite">{item.quantity}</span><button onClick={() => updateQuantity(item.id, item.quantity + 1)} aria-label="Increase quantity"><HiOutlinePlus /></button></div><span className="cart-free">FREE DONATION</span></div></div><button className="cart-remove" onClick={() => removeItem(item.id)} aria-label={`Remove ${item.name}`}><HiOutlineTrash /></button></Motion.article>)}</AnimatePresence></section>
        <aside className="cart-summary"><div className="summary-icon"><HiOutlineShoppingBag /></div><h2>Request summary</h2><p className="summary-intro">Food donations are always free. You only need to confirm the pickup details.</p><div className="summary-lines"><div className="summary-line"><span>Selected listings</span><strong>{items.length}</strong></div><div className="summary-line"><span>Total quantity</span><strong>{totalItems}</strong></div><div className="summary-line"><span>Donation cost</span><strong style={{ color: T.accent }}>Free</strong></div></div><div className="summary-total"><span>Total</span><strong>₹0</strong></div><button className="checkout" onClick={() => navigate("/checkout")}><HiOutlineCheckCircle />Continue to pickup</button><p className="checkout-note"><HiOutlineCheckCircle />No payment details required</p></aside>
      </div>
    </main>}
    {!!items.length && <div className="mobile-checkout"><button className="checkout" onClick={() => navigate("/checkout")}><HiOutlineCheckCircle />Continue · {totalItems} {totalItems === 1 ? "item" : "items"}</button></div>}
  </div>;
}
